/**
 * config.js
 * Single source of truth for API configuration.
 * Change API_BASE here and it propagates everywhere automatically.
 */

export const API_BASE = 'http://127.0.0.1:5000';
export const API_URL  = `${API_BASE}/api`;
